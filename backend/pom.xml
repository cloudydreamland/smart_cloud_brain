<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.smartcloudbrain</groupId>
  <artifactId>smart-cloud-brain</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>smart-cloud-brain</name>
  <description>Smart cloud brain diagnosis platform microservices</description>

  <modules>
    <module>common-lib</module>
    <module>ai-api</module>
    <module>gateway-service</module>
    <module>auth-service</module>
    <module>patient-service</module>
    <module>doctor-service</module>
    <module>registration-service</module>
    <module>triage-service</module>
    <module>medical-record-service</module>
    <module>prescription-service</module>
    <module>ai-service</module>
    <module>notification-service</module>
    <module>admin-service</module>
    <module>coverage-report</module>
  </modules>

  <properties>
    <java.version>17</java.version>
    <spring-boot.version>3.3.6</spring-boot.version>
    <spring-cloud.version>2023.0.4</spring-cloud.version>
    <knife4j.version>4.5.0</knife4j.version>
    <jacoco.version>0.8.12</jacoco.version>
    <maven.compiler.release>${java.version}</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <configuration>
          <excludes>
            <exclude>**/*Application.class</exclude>
            <exclude>**/entity/**</exclude>
            <exclude>**/dto/**</exclude>
            <exclude>**/repository/**</exclude>
            <exclude>**/config/**</exclude>
            <!-- 基础设施：事件发布/订阅框架 -->
            <exclude>**/DomainEventPublisher.class</exclude>
            <exclude>**/DomainEventEnvelope.class</exclude>
            <exclude>**/OutboxEvent.class</exclude>
            <exclude>**/OutboxEventPublisher.class</exclude>
            <!-- 基础设施：微服务间 HTTP 调用客户端 -->
            <exclude>**/InternalDoctorClient.class</exclude>
            <exclude>**/InternalTriageClient.class</exclude>
            <exclude>**/InternalAiClient.class</exclude>
            <exclude>**/InternalNotificationClient.class</exclude>
            <exclude>**/InternalPatientCacheClient.class</exclude>
            <exclude>**/InternalRegistrationCacheClient.class</exclude>
            <!-- 基础设施：安全上下文、健康检查、简单数据类 -->
            <exclude>**/CurrentUserService.class</exclude>
            <exclude>**/AuthenticatedUser.class</exclude>
            <exclude>**/HealthController.class</exclude>
            <exclude>**/PageResult.class</exclude>
            <!-- 基础设施：跨服务共享的网关/AI 调用封装 -->
            <exclude>**/AiGatewayService.class</exclude>
          </excludes>
        </configuration>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>test</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
